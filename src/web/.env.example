# 開発時のみ使用。next dev が /api/* を Workers（wrangler dev）へ同一オリジンとして
# プロキシする際の転送先。未設定時は http://127.0.0.1:8787 を既定値とする。
# 本番はCloudflareのWorker Routeがルーティングするため、このプロキシ設定自体を使わない。
API_ORIGIN=http://127.0.0.1:8787
