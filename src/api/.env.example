# このWorkerは環境変数を wrangler.toml の [vars]（ENVIRONMENT 等）で管理し、
# 本番のシークレットは `wrangler secret put` で設定する（.envは本番には使わない）。
# 現時点で本番シークレットを要する値は存在しない（資格情報を一切保持しない設計のため）。
# .env はローカル開発で wrangler dev に変数を渡したい場合のみ任意で使用できる。
ENVIRONMENT=development
